when:
  - event: push
    branch: main

steps:
  - name: format
    image: ziglang/zig
    commands:
      - echo "Formatting code"
      - zig fmt .

  - name: test
    image: ziglang/zig
    commands:
      - echo "Running tests"
      - zig build test

  - name: build
    image: ziglang/zig
    commands:
      - echo "Starting build"
      - zig build